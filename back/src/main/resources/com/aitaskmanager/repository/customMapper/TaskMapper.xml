<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aitaskmanager.repository.customMapper.TaskMapper">
  <resultMap id="BaseResultMap" type="com.aitaskmanager.repository.model.Tasks">
    <id column="task_sid" jdbcType="INTEGER" property="taskSid" />
    <result column="user_sid" jdbcType="INTEGER" property="userSid" />
    <result column="parent_task_sid" jdbcType="INTEGER" property="parentTaskSid" />
    <result column="title" jdbcType="VARCHAR" property="title" />
    <result column="description" jdbcType="VARCHAR" property="description" />
    <result column="due_date" jdbcType="DATE" property="dueDate" />
    <result column="priority" jdbcType="VARCHAR" property="priority" />
    <result column="status" jdbcType="VARCHAR" property="status" />
    <result column="created_at" jdbcType="TIMESTAMP" property="createdAt" />
    <result column="updated_at" jdbcType="TIMESTAMP" property="updatedAt" />
    <result column="decomposed_at" jdbcType="TIMESTAMP" property="decomposedAt" />
  </resultMap>

  <select id="selectByUserSid" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select task_sid, user_sid, parent_task_sid, title, description, due_date, priority, status, created_at, updated_at, decomposed_at
    from tasks
    where user_sid = #{userSid}
  </select>

  <select id="selectByTaskSidAndUserSid" resultMap="BaseResultMap">
    select task_sid, user_sid, parent_task_sid, title, description, due_date, priority, status, created_at, updated_at, decomposed_at
    from tasks
    where task_sid = #{taskSid}
      and user_sid = #{userSid}
  </select>

  <select id="countChildrenByParentSid" parameterType="java.lang.Integer" resultType="int">
    select count(1)
    from tasks
    where parent_task_sid = #{parentTaskSid}
  </select>

  <select id="selectChildrenByParentSid" parameterType="java.lang.Integer" resultMap="BaseResultMap">
    select task_sid, user_sid, parent_task_sid, title, description, due_date, priority, status, created_at, updated_at, decomposed_at
    from tasks
    where parent_task_sid = #{parentTaskSid}
    order by task_sid asc
  </select>
  
  <insert id="insert" parameterType="com.aitaskmanager.repository.model.Tasks" useGeneratedKeys="true" keyProperty="taskSid" keyColumn="task_sid">
    insert into tasks (user_sid, parent_task_sid, title, description, due_date, priority, status, created_at, updated_at, decomposed_at)
    values (#{userSid}, #{parentTaskSid}, #{title}, #{description}, #{dueDate}, #{priority}, #{status}, now(), now(), #{decomposedAt})
  </insert>
  
  <update id="update">
    update tasks
    set
      title = #{title},
      description = #{description},
      status = #{status},
      priority = #{priority},
      due_date = #{dueDate},
      parent_task_sid = #{parentTaskSid},
      decomposed_at = #{decomposedAt},
      updated_at = now()
    where task_sid = #{taskSid} and user_sid = #{userSid}
  </update>

  <update id="updateDecomposedAt">
    update tasks 
    set decomposed_at = now(),
        updated_at = now() 
    where task_sid = #{taskSid} 
      and user_sid = #{userSid}
  </update>

  <delete id="deleteByTaskSidAndUserSid">
    delete from tasks
    where task_sid = #{taskSid}
      and user_sid = #{userSid}
  </delete>
</mapper>